{
  "id": "execution-env-testability",
  "date": "2026-09-18",
  "tag": "Deep Dive",
  "title": {
    "zh": "ExecutionEnv：让工具测试不再依赖真实文件系统",
    "en": "ExecutionEnv: Making Tool Tests Independent of Real Filesystems"
  },
  "summary": {
    "zh": "rpi-tools 通过 ExecutionEnv trait 隔离了所有环境操作。本文深入解析这个设计如何让工具在测试中使用内存实现，在生产中使用操作系统实现，以及 MutationQueue 如何序列化写操作。",
    "en": "rpi-tools isolates all environment operations through the ExecutionEnv trait. This article dives into how this design lets tools use in-memory implementations in tests and OS implementations in production, and how MutationQueue serializes write operations."
  },
  "body": {
    "zh": [
      "## 问题：工具测试的困境\n\n大多数 coding agent 的工具直接调用 `std::fs::read` 或 `Command::new`。这让测试变得困难：\n- 测试需要真实的文件系统\n- 测试可能意外修改真实文件\n- 测试之间互相干扰\n- CI 环境可能没有某些命令\n\nrpi 的解决方案是**抽象环境操作**。\n\n## ExecutionEnv trait\n\n`ExecutionEnv` 定义了所有环境操作的接口：\n\n```rust\npub trait ExecutionEnv: Send + Sync + 'static {\n    // 文件系统操作\n    fn read_file(&self, path: &Path) -> Result<FileContent, FileError>;\n    fn write_file(&self, path: &Path, content: &str) -> Result<(), FileError>;\n    fn file_info(&self, path: &Path) -> Result<FileInfo, FileError>;\n    \n    // Shell 操作\n    fn execute_shell(&self, command: &str, options: ShellExecOptions) -> Result<ShellOutput, ExecutionError>;\n    \n    // 路径操作\n    fn current_dir(&self) -> Result<PathBuf, ExecutionError>;\n}\n```\n\n两个实现：\n\n```rust\n// 生产环境：真实操作系统\npub struct OsExecutionEnv {\n    cwd: PathBuf,\n}\n\n// 测试环境：内存实现\npub struct InMemoryExecutionEnv {\n    files: Arc<RwLock<HashMap<PathBuf, String>>>,\n    cwd: PathBuf,\n}\n```\n\n## 工具如何使用 ExecutionEnv\n\n工具不直接操作环境，而是通过 `ExecutionToolContext` 持有 `ExecutionEnv`：\n\n```rust\npub struct ExecutionToolContext {\n    env: Arc<dyn ExecutionEnv>,\n    mutation_queue: Arc<MutationQueue>,\n}\n\n// 创建工具时传入 context\npub fn create_read_tool(ctx: &ExecutionToolContext) -> Box<dyn AgentTool> {\n    Box::new(ReadTool {\n        env: ctx.env.clone(),\n    })\n}\n\n// 工具实现\nimpl AgentTool for ReadTool {\n    async fn execute(&self, params: ToolCallParams) -> AgentToolResult {\n        let path = parse_path(&params.arguments)?;\n        match self.env.read_file(&path) {\n            Ok(content) => AgentToolResult::success(content.text),\n            Err(e) => AgentToolResult::error(format!(\"Read failed: {}\", e)),\n        }\n    }\n}\n```\n\n## InMemoryExecutionEnv：测试利器\n\n`InMemoryExecutionEnv` 用 `HashMap` 模拟文件系统：\n\n```rust\npub struct InMemoryExecutionEnv {\n    files: Arc<RwLock<HashMap<PathBuf, String>>>,\n    cwd: PathBuf,\n    shell_scripts: Arc<RwLock<HashMap<String, ShellScript>>>,\n}\n\nimpl ExecutionEnv for InMemoryExecutionEnv {\n    fn read_file(&self, path: &Path) -> Result<FileContent, FileError> {\n        let files = self.files.read().unwrap();\n        files.get(path)\n            .map(|content| FileContent { text: content.clone() })\n            .ok_or(FileError::not_found(path))\n    }\n    \n    fn write_file(&self, path: &Path, content: &str) -> Result<(), FileError> {\n        let mut files = self.files.write().unwrap();\n        files.insert(path.to_path_buf(), content.to_string());\n        Ok(())\n    }\n    \n    fn execute_shell(&self, command: &str, _options: ShellExecOptions) -> Result<ShellOutput, ExecutionError> {\n        let scripts = self.shell_scripts.read().unwrap();\n        scripts.get(command)\n            .map(|script| script.output.clone())\n            .ok_or(ExecutionError::not_found(command))\n    }\n}\n```\n\n## 测试示例\n\n让我们看一个完整的测试：\n\n```rust\n#[tokio::test]\nasync fn test_write_tool_creates_file() {\n    // 1. 创建内存环境\n    let env = Arc::new(InMemoryExecutionEnv::new());\n    \n    // 2. 预置文件（如果需要）\n    env.add_file(Path::new(\"/existing.txt\"), \"existing content\");\n    \n    // 3. 创建工具\n    let ctx = ExecutionToolContext::new(env.clone());\n    let write_tool = create_write_tool(&ctx);\n    \n    // 4. 执行工具\n    let result = write_tool.execute(ToolCallParams {\n        arguments: json!({\n            \"path\": \"/test.txt\",\n            \"content\": \"hello world\"\n        }),\n        ..default()\n    }).await.unwrap();\n    \n    // 5. 验证结果\n    assert!(result.is_success());\n    \n    // 6. 验证文件系统状态\n    let files = env.files();\n    assert_eq!(files.get(Path::new(\"/test.txt\")), Some(&\"hello world\".to_string()));\n}\n\n#[tokio::test]\nasync fn test_bash_tool_with_mock_output() {\n    let env = Arc::new(InMemoryExecutionEnv::new());\n    \n    // 预设 shell 命令的返回\n    env.add_shell_script(\"ls -la\", ShellOutput {\n        stdout: \"total 8\\n-rw-r--r-- 1 user group 123 Jan 1 test.txt\\n\".into(),\n        stderr: \"\".into(),\n        exit_code: 0,\n    });\n    \n    let ctx = ExecutionToolContext::new(env.clone());\n    let bash_tool = create_bash_tool(&ctx, None);\n    \n    let result = bash_tool.execute(ToolCallParams {\n        arguments: json!({ \"command\": \"ls -la\" }),\n        ..default()\n    }).await.unwrap();\n    \n    assert!(result.is_success());\n    assert!(result.content.contains(\"test.txt\"));\n}\n```\n\n## MutationQueue：序列化写操作\n\n并发写同一个文件会导致竞态条件。`MutationQueue` 通过**文件级锁**序列化写操作：\n\n```rust\npub struct MutationQueue {\n    locks: Arc<Mutex<HashMap<PathBuf, Arc<Mutex<()>>>>>,\n}\n\nimpl MutationQueue {\n    // 获取文件锁\n    pub async fn with_file_lock<F, T>(&self, path: &Path, f: F) -> T\n    where\n        F: FnOnce() -> T,\n    {\n        // 获取或创建文件锁\n        let lock = {\n            let mut locks = self.locks.lock().unwrap();\n            locks.entry(path.to_path_buf())\n                .or_insert_with(|| Arc::new(Mutex::new(())))\n                .clone()\n        };\n        \n        // 持有锁执行操作\n        let _guard = lock.lock().await;\n        f()\n    }\n}\n\n// Write 工具使用 MutationQueue\nimpl AgentTool for WriteTool {\n    async fn execute(&self, params: ToolCallParams) -> AgentToolResult {\n        let path = parse_path(&params.arguments)?;\n        let content = parse_content(&params.arguments)?;\n        \n        // 通过 MutationQueue 序列化\n        self.ctx.mutation_queue()\n            .with_file_lock(&path, || {\n                self.ctx.env().write_file(&path, &content)\n            })\n            .await\n            .map_err(|e| AgentToolError::from(e))\n    }\n}\n```\n\n这确保了对同一文件的并发写操作会按顺序执行，避免数据损坏。\n\n## 路径安全\n\n`ExecutionEnv` 还负责路径安全检查：\n\n```rust\nimpl OsExecutionEnv {\n    fn resolve_path(&self, path: &Path) -> Result<PathBuf, FileError> {\n        let resolved = self.cwd.join(path);\n        \n        // 防止路径遍历攻击\n        if !resolved.starts_with(&self.cwd) {\n            return Err(FileError::forbidden(\"path traversal detected\"));\n        }\n        \n        // 规范化路径\n        normalize_tool_path(&resolved)\n    }\n}\n```\n\n## 实际生产使用\n\n在 CLI 中，使用 `OsExecutionEnv`：\n\n```rust\n// rpi-cli 中\nlet env = Arc::new(OsExecutionEnv::with_cwd(current_dir()?));\nlet ctx = ExecutionToolContext::new(env.clone());\n\nlet read = create_read_tool(&ctx);\nlet write = create_write_tool(&ctx);\nlet edit = create_edit_tool(&ctx);\nlet bash = create_bash_tool(&ctx, None);\n\nlet agent = Agent::builder(model)\n    .tools(vec![read, write, edit, bash])\n    .build();\n```\n\n在测试中，使用 `InMemoryExecutionEnv`：\n\n```rust\n// 测试中\nlet env = Arc::new(InMemoryExecutionEnv::new());\nlet ctx = ExecutionToolContext::new(env.clone());\n\n// 同样的工具，不同的环境\nlet read = create_read_tool(&ctx);\n// ...\n```\n\n## 总结\n\n`ExecutionEnv` 的设计让 rpi 的工具：\n\n1. **可测试**：内存实现让测试快速、隔离、可重复\n2. **安全**：路径检查防止越权访问\n3. **并发安全**：MutationQueue 序列化写操作\n4. **可替换**：可以轻松添加新的环境实现（如 Docker 环境、远程环境）\n\n这种设计模式值得在所有需要环境操作的 Rust 项目中推广。"
    ],
    "en": [
      "## The Problem: Tool Testing Dilemma\n\nMost coding agent tools directly call `std::fs::read` or `Command::new`. This makes testing difficult:\n- Tests need a real filesystem\n- Tests might accidentally modify real files\n- Tests interfere with each other\n- CI environments might lack certain commands\n\nrpi's solution is to **abstract environment operations**.\n\n## ExecutionEnv trait\n\n`ExecutionEnv` defines the interface for all environment operations:\n\n```rust\npub trait ExecutionEnv: Send + Sync + 'static {\n    // Filesystem operations\n    fn read_file(&self, path: &Path) -> Result<FileContent, FileError>;\n    fn write_file(&self, path: &Path, content: &str) -> Result<(), FileError>;\n    fn file_info(&self, path: &Path) -> Result<FileInfo, FileError>;\n    \n    // Shell operations\n    fn execute_shell(&self, command: &str, options: ShellExecOptions) -> Result<ShellOutput, ExecutionError>;\n    \n    // Path operations\n    fn current_dir(&self) -> Result<PathBuf, ExecutionError>;\n}\n```\n\nTwo implementations:\n\n```rust\n// Production: real OS\npub struct OsExecutionEnv {\n    cwd: PathBuf,\n}\n\n// Testing: in-memory\npub struct InMemoryExecutionEnv {\n    files: Arc<RwLock<HashMap<PathBuf, String>>>,\n    cwd: PathBuf,\n}\n```\n\n## How Tools Use ExecutionEnv\n\nTools don't directly operate on the environment; they hold `ExecutionEnv` through `ExecutionToolContext`:\n\n```rust\npub struct ExecutionToolContext {\n    env: Arc<dyn ExecutionEnv>,\n    mutation_queue: Arc<MutationQueue>,\n}\n\n// Pass context when creating tools\npub fn create_read_tool(ctx: &ExecutionToolContext) -> Box<dyn AgentTool> {\n    Box::new(ReadTool {\n        env: ctx.env.clone(),\n    })\n}\n\n// Tool implementation\nimpl AgentTool for ReadTool {\n    async fn execute(&self, params: ToolCallParams) -> AgentToolResult {\n        let path = parse_path(&params.arguments)?;\n        match self.env.read_file(&path) {\n            Ok(content) => AgentToolResult::success(content.text),\n            Err(e) => AgentToolResult::error(format!(\"Read failed: {}\", e)),\n        }\n    }\n}\n```\n\n## InMemoryExecutionEnv: Testing Powerhouse\n\n`InMemoryExecutionEnv` simulates a filesystem with `HashMap`:\n\n```rust\npub struct InMemoryExecutionEnv {\n    files: Arc<RwLock<HashMap<PathBuf, String>>>,\n    cwd: PathBuf,\n    shell_scripts: Arc<RwLock<HashMap<String, ShellScript>>>,\n}\n\nimpl ExecutionEnv for InMemoryExecutionEnv {\n    fn read_file(&self, path: &Path) -> Result<FileContent, FileError> {\n        let files = self.files.read().unwrap();\n        files.get(path)\n            .map(|content| FileContent { text: content.clone() })\n            .ok_or(FileError::not_found(path))\n    }\n    \n    fn write_file(&self, path: &Path, content: &str) -> Result<(), FileError> {\n        let mut files = self.files.write().unwrap();\n        files.insert(path.to_path_buf(), content.to_string());\n        Ok(())\n    }\n    \n    fn execute_shell(&self, command: &str, _options: ShellExecOptions) -> Result<ShellOutput, ExecutionError> {\n        let scripts = self.shell_scripts.read().unwrap();\n        scripts.get(command)\n            .map(|script| script.output.clone())\n            .ok_or(ExecutionError::not_found(command))\n    }\n}\n```\n\n## Testing Example\n\nLet's look at a complete test:\n\n```rust\n#[tokio::test]\nasync fn test_write_tool_creates_file() {\n    // 1. Create in-memory environment\n    let env = Arc::new(InMemoryExecutionEnv::new());\n    \n    // 2. Pre-populate files (if needed)\n    env.add_file(Path::new(\"/existing.txt\"), \"existing content\");\n    \n    // 3. Create tool\n    let ctx = ExecutionToolContext::new(env.clone());\n    let write_tool = create_write_tool(&ctx);\n    \n    // 4. Execute tool\n    let result = write_tool.execute(ToolCallParams {\n        arguments: json!({\n            \"path\": \"/test.txt\",\n            \"content\": \"hello world\"\n        }),\n        ..default()\n    }).await.unwrap();\n    \n    // 5. Verify result\n    assert!(result.is_success());\n    \n    // 6. Verify filesystem state\n    let files = env.files();\n    assert_eq!(files.get(Path::new(\"/test.txt\")), Some(&\"hello world\".to_string()));\n}\n\n#[tokio::test]\nasync fn test_bash_tool_with_mock_output() {\n    let env = Arc::new(InMemoryExecutionEnv::new());\n    \n    // Preset shell command returns\n    env.add_shell_script(\"ls -la\", ShellOutput {\n        stdout: \"total 8\\n-rw-r--r-- 1 user group 123 Jan 1 test.txt\\n\".into(),\n        stderr: \"\".into(),\n        exit_code: 0,\n    });\n    \n    let ctx = ExecutionToolContext::new(env.clone());\n    let bash_tool = create_bash_tool(&ctx, None);\n    \n    let result = bash_tool.execute(ToolCallParams {\n        arguments: json!({ \"command\": \"ls -la\" }),\n        ..default()\n    }).await.unwrap();\n    \n    assert!(result.is_success());\n    assert!(result.content.contains(\"test.txt\"));\n}\n```\n\n## MutationQueue: Serializing Write Operations\n\nConcurrent writes to the same file cause race conditions. `MutationQueue` serializes write operations through **file-level locking**:\n\n```rust\npub struct MutationQueue {\n    locks: Arc<Mutex<HashMap<PathBuf, Arc<Mutex<()>>>>>,\n}\n\nimpl MutationQueue {\n    // Acquire file lock\n    pub async fn with_file_lock<F, T>(&self, path: &Path, f: F) -> T\n    where\n        F: FnOnce() -> T,\n    {\n        // Get or create file lock\n        let lock = {\n            let mut locks = self.locks.lock().unwrap();\n            locks.entry(path.to_path_buf())\n                .or_insert_with(|| Arc::new(Mutex::new(())))\n                .clone()\n        };\n        \n        // Hold lock while executing operation\n        let _guard = lock.lock().await;\n        f()\n    }\n}\n\n// Write tool uses MutationQueue\nimpl AgentTool for WriteTool {\n    async fn execute(&self, params: ToolCallParams) -> AgentToolResult {\n        let path = parse_path(&params.arguments)?;\n        let content = parse_content(&params.arguments)?;\n        \n        // Serialize through MutationQueue\n        self.ctx.mutation_queue()\n            .with_file_lock(&path, || {\n                self.ctx.env().write_file(&path, &content)\n            })\n            .await\n            .map_err(|e| AgentToolError::from(e))\n    }\n}\n```\n\nThis ensures concurrent write operations to the same file execute sequentially, avoiding data corruption.\n\n## Path Safety\n\n`ExecutionEnv` also handles path safety checks:\n\n```rust\nimpl OsExecutionEnv {\n    fn resolve_path(&self, path: &Path) -> Result<PathBuf, FileError> {\n        let resolved = self.cwd.join(path);\n        \n        // Prevent path traversal attacks\n        if !resolved.starts_with(&self.cwd) {\n            return Err(FileError::forbidden(\"path traversal detected\"));\n        }\n        \n        // Normalize path\n        normalize_tool_path(&resolved)\n    }\n}\n```\n\n## Production Usage\n\nIn the CLI, use `OsExecutionEnv`:\n\n```rust\n// In rpi-cli\nlet env = Arc::new(OsExecutionEnv::with_cwd(current_dir()?));\nlet ctx = ExecutionToolContext::new(env.clone());\n\nlet read = create_read_tool(&ctx);\nlet write = create_write_tool(&ctx);\nlet edit = create_edit_tool(&ctx);\nlet bash = create_bash_tool(&ctx, None);\n\nlet agent = Agent::builder(model)\n    .tools(vec![read, write, edit, bash])\n    .build();\n```\n\nIn tests, use `InMemoryExecutionEnv`:\n\n```rust\n// In tests\nlet env = Arc::new(InMemoryExecutionEnv::new());\nlet ctx = ExecutionToolContext::new(env.clone());\n\n// Same tools, different environment\nlet read = create_read_tool(&ctx);\n// ...\n```\n\n## Summary\n\n`ExecutionEnv` design makes rpi's tools:\n\n1. **Testable**: In-memory implementation makes tests fast, isolated, repeatable\n2. **Safe**: Path checks prevent unauthorized access\n3. **Concurrency-safe**: MutationQueue serializes write operations\n4. **Replaceable**: Easy to add new environment implementations (Docker, remote, etc.)\n\nThis design pattern deserves promotion in all Rust projects requiring environment operations."
    ]
  },
  "highlights": {
    "zh": ["ExecutionEnv trait", "内存测试环境", "MutationQueue", "路径安全"],
    "en": ["ExecutionEnv trait", "In-memory testing", "MutationQueue", "Path safety"]
  }
}
