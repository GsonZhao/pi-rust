{
  "id": "plugin-abi-stable-boundary",
  "date": "2026-09-20",
  "tag": "Deep Dive",
  "title": {
    "zh": "插件 ABI：跨 Rust 版本的稳定二进制边界",
    "en": "Plugin ABI: A Stable Binary Boundary Across Rust Versions"
  },
  "summary": {
    "zh": "rpi 的插件系统使用 #[repr(C)] ABI 让宿主和插件可以用不同 Rust 版本编译。本文深入解析这个边界的设计：为什么不用 Rust trait、如何处理内存所有权、以及如何防止 panic 跨越 FFI。",
    "en": "rpi's plugin system uses #[repr(C)] ABI to allow host and plugins to be compiled with different Rust versions. This article dives into the boundary design: why not Rust traits, how memory ownership is handled, and how panics are prevented from crossing FFI."
  },
  "body": {
    "zh": [
      "## 为什么不用 Rust trait？\n\nRust 的 trait 是零成本抽象，但它们有一个致命限制：**不同 Rust 版本编译的 crate 之间不兼容**。如果宿主用 Rust 1.78 编译，插件用 Rust 1.80 编译，trait 对象的在内存中的表示可能不同。\n\n这意味着我们不能简单地定义一个 `trait Tool { fn execute(...) }` 然后让插件实现它。我们需要一个**稳定的二进制接口**。\n\n## #[repr(C)]：Rust 的 FFI 契约\n\n解决方案是 `#[repr(C)]`。它告诉 Rust 编译器：\"按照 C 的方式排列这个结构体的内存布局\"。C ABI 是稳定的——无论哪个版本的 Rust 编译，`#[repr(C)]` 结构体的内存布局都是一样的。\n\n```rust\n// rpi-plugin-sdk 中的核心类型\n#[repr(C)]\npub struct StbString {\n    ptr: *mut u8,\n    len: usize,\n}\n\n// 为什么不用 String？\n// String 有 Drop impl，跨 FFI 会导致 double-free\n// StbString 是 Copy 的，只复制指针，不复制分配\n```\n\n## 内存所有权：谁分配，谁释放\n\n跨 FFI 边界的最棘手问题是内存所有权。rpi 的规则很简单：\n\n**规则 1：没有 Drop 类型跨越边界**\n\n`String`、`Vec`、`serde_json::Value` 都有 `Drop` impl。如果它们跨越 FFI，两边都可能尝试释放同一块内存。\n\n```rust\n// ❌ 错误：String 有 Drop\n#[repr(C)]\npub struct BadToolResult {\n    content: String,  // 谁释放？\n}\n\n// ✅ 正确：StbString 是 Copy\n#[repr(C)]\npub struct GoodToolResult {\n    content: StbString,  // 显式 free_string 释放\n}\n```\n\n**规则 2：结构化数据用 JSON 序列化**\n\n工具参数、工具结果、事件等复杂数据通过 JSON 字符串传递。这牺牲了一些性能，但保证了兼容性。\n\n```rust\n// 工具参数从模型来，是 JSON\n#[repr(C)]\npub struct ToolExecuteParams {\n    tool_call_id: StbString,\n    arguments_json: StbString,  // JSON 字符串\n}\n\n// 工具结果也是 JSON\n#[repr(C)]\npub struct ToolExecuteResult {\n    content_json: StbString,\n    is_error: bool,\n}\n```\n\n## 工具执行：4 函数协议\n\n一个插件工具通过 4 个导出函数驱动执行：\n\n```rust\n// 1. execute: 创建执行句柄\npub type ToolExecuteFn = unsafe extern \"C\" fn(\n    params: *const ToolExecuteParams,\n) -> *mut c_void;  // 句柄\n\n// 2. poll: 非阻塞轮询状态\npub type ToolPollFn = unsafe extern \"C\" fn(\n    handle: *mut c_void,\n) -> StepResult;  // Pending/Done/Err\n\n// 3. cancel: 设置取消标志（幂等、线程安全）\npub type ToolCancelFn = unsafe extern \"C\" fn(\n    handle: *mut c_void,\n);\n\n// 4. destroy: 释放句柄（恰好调用一次）\npub type ToolDestroyFn = unsafe extern \"C\" fn(\n    handle: *mut c_void,\n);\n```\n\n关键设计：`cancel` ≠ `destroy`。`cancel` 是幂等的标志设置，`destroy` 是恰好一次的释放。混淆它们会导致 UAF 或 double-free。\n\n## 防止 panic 跨越 FFI\n\nRust 的 panic 是 unwind，如果它跨越 FFI 边界，行为是未定义的。rpi 在每一侧都用 `catch_unwind` 包裹：\n\n```rust\n// 宿主调用插件时\nlet result = std::panic::catch_unwind(|| {\n    unsafe { plugin_execute(params) }\n});\nmatch result {\n    Ok(value) => value,\n    Err(_) => {\n        // 插件 panic 了，记录日志，返回错误\n        ToolExecuteResult::error(\"plugin panicked\")\n    }\n}\n\n// 插件回调宿主时（如事件发射）\nlet result = std::panic::catch_unwind(|| {\n    unsafe { host_emit_event(event) }\n});\n// 同样处理\n```\n\n这确保了一侧的 panic 不会污染另一侧。\n\n## 实际插件示例\n\n让我们看一个完整的插件实现：\n\n```rust\n// Cargo.toml\n[lib]\ncrate-type = [\"cdylib\"]\n\n[dependencies]\nrpi-plugin-sdk = \"0.1\"\n\n// src/lib.rs\nuse rpi_plugin_sdk::*;\n\n#[no_mangle]\npub extern \"C\" fn rpi_plugin_register(api: *const PluginApiVt) {\n    let api = unsafe { &*api };\n    \n    // 注册一个 echo 工具\n    api.register_tool(ToolRegistration {\n        name: StbString::from(\"echo\"),\n        description: StbString::from(\"Echoes input\"),\n        parameters_json: StbString::from(r#\"{\"type\":\"object\",\"properties\":{\"message\":{\"type\":\"string\"}}}\"#),\n        execute: my_execute,\n        poll: my_poll,\n        cancel: my_cancel,\n        destroy: my_destroy,\n    });\n}\n\n// 执行函数：分配句柄\n#[no_mangle]\npub extern \"C\" fn my_execute(params: *const ToolExecuteParams) -> *mut c_void {\n    let params = unsafe { &*params };\n    let args: serde_json::Value = serde_json::from_str(\n        unsafe { params.arguments_json.to_str() }\n    ).unwrap();\n    \n    let state = Box::new(MyToolState {\n        message: args[\"message\"].as_str().unwrap_or(\"\").to_string(),\n        done: false,\n    });\n    Box::into_raw(state) as *mut c_void\n}\n\n// 轮询函数：返回结果\n#[no_mangle]\npub extern \"C\" fn my_poll(handle: *mut c_void) -> StepResult {\n    let state = unsafe { &mut *(handle as *mut MyToolState) };\n    if state.done {\n        StepResult::done(StbString::from(&state.message))\n    } else {\n        state.done = true;\n        StepResult::pending(StbString::from(\"processing...\"))\n    }\n}\n\n// 取消函数：设置标志\n#[no_mangle]\npub extern \"C\" fn my_cancel(handle: *mut c_void) {\n    let state = unsafe { &mut *(handle as *mut MyToolState) };\n    state.cancelled = true;\n}\n\n// 销毁函数：释放内存\n#[no_mangle]\npub extern \"C\" fn my_destroy(handle: *mut c_void) {\n    if !handle.is_null() {\n        drop(unsafe { Box::from_raw(handle as *mut MyToolState) });\n    }\n}\n\nstruct MyToolState {\n    message: String,\n    done: bool,\n    cancelled: bool,\n}\n```\n\n## 宿主侧：异步桥接\n\n插件是同步的（C ABI 不支持 async），但 Agent loop 是异步的。`rpi-extensions` 通过 `spawn_blocking` 桥接：\n\n```rust\n// 宿主侧的 PluginToolAdapter\nimpl AgentTool for PluginToolAdapter {\n    async fn execute(&self, params: ToolCallParams, ...) -> AgentToolResult {\n        // 1. 调用插件的 execute，获取句柄\n        let handle = unsafe { (self.execute_fn)(&params) };\n        \n        // 2. 在 blocking 线程中轮询\n        let result = tokio::task::spawn_blocking(move || {\n            loop {\n                let step = unsafe { (self.poll_fn)(handle) };\n                match step {\n                    StepResult::Done(result) => break result,\n                    StepResult::Err(e) => break e,\n                    StepResult::Pending(partial) => {\n                        // 转发部分更新\n                        on_update(partial);\n                        std::thread::sleep(Duration::from_millis(10));\n                    }\n                }\n            }\n        }).await;\n        \n        // 3. 确保 destroy 恰好调用一次\n        unsafe { (self.destroy_fn)(handle) };\n        \n        result\n    }\n}\n```\n\n## 总结\n\nrpi 的插件 ABI 设计遵循几个核心原则：\n\n1. **稳定性**：`#[repr(C)]` 保证内存布局跨版本稳定\n2. **安全性**：没有 Drop 类型跨越边界，显式释放\n3. **隔离性**：`catch_unwind` 防止 panic 传播\n4. **简洁性**：4 函数协议覆盖所有执行场景\n\n这种设计让 rpi 的插件生态可以独立演进：插件作者可以用任何 Rust 版本，宿主也可以用任何 Rust 版本，只要它们都遵循同一个 ABI 合约。"
    ],
    "en": [
      "## Why Not Rust Traits?\n\nRust traits are zero-cost abstractions, but they have a fatal limitation: **crates compiled with different Rust versions are incompatible**. If the host is compiled with Rust 1.78 and the plugin with Rust 1.80, the in-memory representation of trait objects may differ.\n\nThis means we can't simply define `trait Tool { fn execute(...) }` and let plugins implement it. We need a **stable binary interface**.\n\n## #[repr(C)]: Rust's FFI Contract\n\nThe solution is `#[repr(C)]`. It tells the Rust compiler: \"Arrange this struct's memory layout the C way\". The C ABI is stable—regardless of which Rust version compiles it, a `#[repr(C)]` struct has the same memory layout.\n\n```rust\n// Core type in rpi-plugin-sdk\n#[repr(C)]\npub struct StbString {\n    ptr: *mut u8,\n    len: usize,\n}\n\n// Why not String?\n// String has Drop impl, crossing FFI would cause double-free\n// StbString is Copy, only copies the pointer, not the allocation\n```\n\n## Memory Ownership: Who Allocates, Who Frees\n\nThe trickiest problem crossing FFI boundaries is memory ownership. rpi's rules are simple:\n\n**Rule 1: No Drop types cross the boundary**\n\n`String`, `Vec`, `serde_json::Value` all have `Drop` impls. If they cross FFI, both sides might try to free the same memory.\n\n```rust\n// ❌ Wrong: String has Drop\n#[repr(C)]\npub struct BadToolResult {\n    content: String,  // Who frees?\n}\n\n// ✅ Correct: StbString is Copy\n#[repr(C)]\npub struct GoodToolResult {\n    content: StbString,  // Explicit free_string to release\n}\n```\n\n**Rule 2: Structured data uses JSON serialization**\n\nComplex data like tool parameters, tool results, and events are passed via JSON strings. This sacrifices some performance but guarantees compatibility.\n\n```rust\n// Tool parameters come from the model, as JSON\n#[repr(C)]\npub struct ToolExecuteParams {\n    tool_call_id: StbString,\n    arguments_json: StbString,  // JSON string\n}\n\n// Tool results are also JSON\n#[repr(C)]\npub struct ToolExecuteResult {\n    content_json: StbString,\n    is_error: bool,\n}\n```\n\n## Tool Execution: The 4-Function Protocol\n\nA plugin tool drives execution through 4 exported functions:\n\n```rust\n// 1. execute: Create execution handle\npub type ToolExecuteFn = unsafe extern \"C\" fn(\n    params: *const ToolExecuteParams,\n) -> *mut c_void;  // handle\n\n// 2. poll: Non-blocking status check\npub type ToolPollFn = unsafe extern \"C\" fn(\n    handle: *mut c_void,\n) -> StepResult;  // Pending/Done/Err\n\n// 3. cancel: Set cancel flag (idempotent, thread-safe)\npub type ToolCancelFn = unsafe extern \"C\" fn(\n    handle: *mut c_void,\n);\n\n// 4. destroy: Free handle (called exactly once)\npub type ToolDestroyFn = unsafe extern \"C\" fn(\n    handle: *mut c_void,\n);\n```\n\nKey design: `cancel` ≠ `destroy`. `cancel` is an idempotent flag set, `destroy` is a exactly-once free. Confusing them leads to UAF or double-free.\n\n## Preventing Panics from Crossing FFI\n\nRust panics are unwinds, and if they cross FFI boundaries, behavior is undefined. rpi wraps every side with `catch_unwind`:\n\n```rust\n// Host calling plugin\nlet result = std::panic::catch_unwind(|| {\n    unsafe { plugin_execute(params) }\n});\nmatch result {\n    Ok(value) => value,\n    Err(_) => {\n        // Plugin panicked, log and return error\n        ToolExecuteResult::error(\"plugin panicked\")\n    }\n}\n\n// Plugin calling back to host (e.g., event emission)\nlet result = std::panic::catch_unwind(|| {\n    unsafe { host_emit_event(event) }\n});\n// Same handling\n```\n\nThis ensures one side's panic doesn't contaminate the other.\n\n## Practical Plugin Example\n\nLet's look at a complete plugin implementation:\n\n```rust\n// Cargo.toml\n[lib]\ncrate-type = [\"cdylib\"]\n\n[dependencies]\nrpi-plugin-sdk = \"0.1\"\n\n// src/lib.rs\nuse rpi_plugin_sdk::*;\n\n#[no_mangle]\npub extern \"C\" fn rpi_plugin_register(api: *const PluginApiVt) {\n    let api = unsafe { &*api };\n    \n    // Register an echo tool\n    api.register_tool(ToolRegistration {\n        name: StbString::from(\"echo\"),\n        description: StbString::from(\"Echoes input\"),\n        parameters_json: StbString::from(r#\"{\"type\":\"object\",\"properties\":{\"message\":{\"type\":\"string\"}}}\"#),\n        execute: my_execute,\n        poll: my_poll,\n        cancel: my_cancel,\n        destroy: my_destroy,\n    });\n}\n\n// Execute function: allocate handle\n#[no_mangle]\npub extern \"C\" fn my_execute(params: *const ToolExecuteParams) -> *mut c_void {\n    let params = unsafe { &*params };\n    let args: serde_json::Value = serde_json::from_str(\n        unsafe { params.arguments_json.to_str() }\n    ).unwrap();\n    \n    let state = Box::new(MyToolState {\n        message: args[\"message\"].as_str().unwrap_or(\"\").to_string(),\n        done: false,\n    });\n    Box::into_raw(state) as *mut c_void\n}\n\n// Poll function: return result\n#[no_mangle]\npub extern \"C\" fn my_poll(handle: *mut c_void) -> StepResult {\n    let state = unsafe { &mut *(handle as *mut MyToolState) };\n    if state.done {\n        StepResult::done(StbString::from(&state.message))\n    } else {\n        state.done = true;\n        StepResult::pending(StbString::from(\"processing...\"))\n    }\n}\n\n// Cancel function: set flag\n#[no_mangle]\npub extern \"C\" fn my_cancel(handle: *mut c_void) {\n    let state = unsafe { &mut *(handle as *mut MyToolState) };\n    state.cancelled = true;\n}\n\n// Destroy function: free memory\n#[no_mangle]\npub extern \"C\" fn my_destroy(handle: *mut c_void) {\n    if !handle.is_null() {\n        drop(unsafe { Box::from_raw(handle as *mut MyToolState) });\n    }\n}\n\nstruct MyToolState {\n    message: String,\n    done: bool,\n    cancelled: bool,\n}\n```\n\n## Host Side: Async Bridging\n\nPlugins are synchronous (C ABI doesn't support async), but the Agent loop is async. `rpi-extensions` bridges via `spawn_blocking`:\n\n```rust\n// Host-side PluginToolAdapter\nimpl AgentTool for PluginToolAdapter {\n    async fn execute(&self, params: ToolCallParams, ...) -> AgentToolResult {\n        // 1. Call plugin's execute, get handle\n        let handle = unsafe { (self.execute_fn)(&params) };\n        \n        // 2. Poll in blocking thread\n        let result = tokio::task::spawn_blocking(move || {\n            loop {\n                let step = unsafe { (self.poll_fn)(handle) };\n                match step {\n                    StepResult::Done(result) => break result,\n                    StepResult::Err(e) => break e,\n                    StepResult::Pending(partial) => {\n                        // Forward partial updates\n                        on_update(partial);\n                        std::thread::sleep(Duration::from_millis(10));\n                    }\n                }\n            }\n        }).await;\n        \n        // 3. Ensure destroy is called exactly once\n        unsafe { (self.destroy_fn)(handle) };\n        \n        result\n    }\n}\n```\n\n## Summary\n\nrpi's plugin ABI design follows several core principles:\n\n1. **Stability**: `#[repr(C)]` guarantees memory layout stability across versions\n2. **Safety**: No Drop types cross the boundary, explicit freeing\n3. **Isolation**: `catch_unwind` prevents panic propagation\n4. **Simplicity**: 4-function protocol covers all execution scenarios\n\nThis design lets rpi's plugin ecosystem evolve independently: plugin authors can use any Rust version, the host can use any Rust version, as long as they both follow the same ABI contract."
    ]
  },
  "highlights": {
    "zh": ["#[repr(C)] ABI", "4 函数协议", "catch_unwind", "内存所有权"],
    "en": ["#[repr(C)] ABI", "4-function protocol", "catch_unwind", "Memory ownership"]
  }
}
